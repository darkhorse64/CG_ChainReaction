read color:word(1)

gameloop
loop 8 read cells:word(50)
write random

INPUT
color: your color (r=red, b=blue)
cells: 8 space-separated tokens per row (. or r1-r3 or b1-b3)

OUTPUT
e.g. 3 5 (place an orb on row 3, column 5)
or: random (let the referee pick a random valid move)
